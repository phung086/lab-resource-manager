# Current verified gap matrix — 2026-09-24

## Baseline

- Checkout: `main` at `dbab29455994f0cd4953a00018896d47dc84344b`; worktree was clean before this report.
- External review: `docs/EXTERNAL_BUSINESS_UX_REVIEW_20260924.md` is in open PR [#5](https://github.com/phung086/lab-resource-manager/pull/5), commit `5faa0ac`, and is not yet in `main`. It is an input, not implementation authority.
- General [CI run 35894836193](https://github.com/phung086/lab-resource-manager/actions/runs/35894836193) passed at this HEAD. [Batch 8 release gate 35894836190](https://github.com/phung086/lab-resource-manager/actions/runs/35894836190), [Batch 7 demo 35894836158](https://github.com/phung086/lab-resource-manager/actions/runs/35894836158), and [Batch 7 regression 35894836132](https://github.com/phung086/lab-resource-manager/actions/runs/35894836132) failed. GitHub job metadata identifies migration deployment/startup steps as the failure points; the external review reports the specific `Prior migration checksum/history precondition failed` error in Batch 8.
- Local static baseline: backend `npm test` 27/27, backend lint, frontend lint (0 errors, 13 existing warnings), frontend typecheck/build, and `prisma validate` passed. These checks do not establish database or end-to-end correctness.
- No PostgreSQL listener was found on local ports 5432 or 15436. Docker Desktop was started for this audit, but its Linux engine did not respond to `docker info`; local `psql`/`initdb` are PostgreSQL 18, not the required 16. A fresh PostgreSQL 16 reproduction and `_prisma_migrations` inspection remain unverified locally.

## Current verified gap matrix

### Migration byte comparison

The SHA-256 values below compare the embedded predecessor checksum with a fresh checkout representation of each tracked Git blob under `eol=crlf`. This is a read-only byte calculation, not a PostgreSQL migration result.

| Migration | Embedded / existing Windows worktree | Fresh CRLF checkout |
| --- | --- | --- |
| `20260723000100_init` | `c942177016dabbfa0c3465a5e923bd0edf490f238021b6ca96ab65cdca03be96` | `f87d8e476d5f76181b8c1dd505683996edf84370c538bf91108ca83b07c97318` |
| `20260723000200_remove_resource_owner_default` | `66a1614148472e82df95e52c3649e6119a54c9029fc0d4b4bc71e25a688939aa` | `2fc2edcf54ac2cbb06a672c14e45c0508d651bd5aebffc82ae5ba067e5ca6cc0` |
| `20260723000300_require_explicit_resource_fields` | `df6b0f54b676d92cda13592f3c55bad7b829d9f79058bc1092bf4894e03b9fa7` | `a28bfcaacae2b209a8393855b575d5859c8250dd30f892787328fd0246077223` |
| `20260727000100_add_i18n_message_keys` | `10b67a12359efa91038b95890c1505666e10648d7dd6a12823088544df2c6ed0` | `8f31d1a9d5c8c36993c2523cd1008774154a1935c7525e04221c41d015b5b316` |
| `20260817000100_add_maintenance_windows` | `030a4964d697a5b847f94e38e8e5bbecbf921808867a73f15232864b9597022f` | `9147d901f4196afc5192914df4f1658bbb5697b09c2fed87282bc010fe8ab13d` |
| `20260820000100_add_booking_guard_constraint` | `658716b3be743f2bbd81b1c2f4dee5248eb1b9751a8eea693ed19b5a2b32eedb` | `212fdd145e5ce66b653ebb37361e056d253ca73678e8e3d1e69b9d8c2f597bd4` |
| `20260820000200_add_orchestration_provenance_and_policy` | `ca0da76bb6ef6c8d1f04a518cc8398446e499c0c11bcf70e3155937b61b691c3` | `3226d13bbe70f28e6b83859e3cb98cf606304478cda434cfcbe0d2e21ba3f89a` |


| ID | Area | Current evidence | Risk | Priority | Proposed fix | Requires business decision |
| --- | --- | --- | --- | --- | --- | --- |
| G-01 | Fresh DB migration | `batch8-release-gate.yml` runs fresh PostgreSQL 16 migration and fails; normal `ci.yml` has no migration job. The seven Windows worktree SQL hashes match the reconciliation migration's embedded hashes; the first five match `docs/BATCH1B_VERIFICATION.json`. However, the first SQL file has 163 CRLF and 4 LF-only lines, whereas Git stores 167 LF lines and `.gitattributes` instructs fresh checkout to use CRLF. Reconstructing fresh checkout bytes from each Git blob gives a different SHA-256 for **all seven** predecessor files. This explains the reconciliation precondition failure; the actual Prisma-recorded values still need PostgreSQL 16 confirmation. | Clean deployment and DB integration are blocked; a green general CI is misleading. | P0 | Reproduce on isolated PostgreSQL 16, capture Prisma-recorded checksums, select a safe clean-deploy strategy without editing applied SQL/history, then add a required CI gate. | Yes: approve the canonical clean-deploy strategy after evidence. |
| G-02 | Training eligibility | `schema.prisma` has `TrainingRequirement` and `UserCertification`; `createBooking` in `bookingService.js` checks quote, resource state, LabPolicy and availability, but never queries training requirements or certificates. | A user without mandatory training can reserve a restricted resource. | P0 | Enforce all mandatory active certifications inside booking authority and test expired/revoked/other-user/multiple-course cases. | Training applicability per resource and override policy need explicit ownership; default is no override. |
| G-03 | Guest booking atomicity | `guestBookingService.js` consumes OTP and creates/updates user in one transaction, then calls `createBooking()` in a second transaction. | Conflict, stale quote or policy denial leaves consumed OTP and mutated/account data without a booking. | P0 | Put identity, OTP consumption and booking creation behind one coherent transaction boundary while retaining DB overlap protection; test rollback cases. | No for consistency behavior. |
| G-04 | Existing identity classification | Guest completion always writes `customerType: "EXTERNAL"` to an existing email's user record. Registration and `PATCH /users/me` also accept self-supplied customer type. | Trusted internal classification can be overwritten and later used for pricing/priority. | P0/P1 | Preserve existing classification in guest flow; separate declared from verified business identity before using type as an entitlement. | Yes: who verifies INTERNAL/EXTERNAL. |
| G-05 | OTP attempt and resend controls | `verifyGuestOtp` increments attempts inside the same interactive transaction that then throws, so the increment rolls back on an invalid code. `sendGuestBookingOtp` inserts new rows without invalidating previous challenges or applying a per-email cooldown. | The five-attempt lock is ineffective; multiple live codes and email abuse remain possible. | P1 security | Persist failed attempts outside a rolled-back booking transaction, allow one active challenge per purpose/email, add resend/email/IP throttles and focused tests. | No. |
| G-06 | Temporary password | New guest account hashes the phone number as password and sets `passwordResetRequired=true`; `requireAuth` does not enforce that flag across sensitive routes. | Predictable credential remains useful beyond first login. | P1 security | Honor the owner's phone-password decision as temporary only: server gate sensitive operations until password change, apply expiry/rate limits, or implement OTP-based password setup if approved. | Yes if replacing the explicitly requested phone-password behavior. |
| G-07 | Payment session and late IPN | `initiatePayment` returns a stored `paymentUrl` whenever provider matches, with no persisted expiry. `processVnpayIpn` updates pending transaction without checking whether linked booking was cancelled. | Expired URL can be reused; late success has no refund/reconciliation state. | P1; P0 if live payment demo | Persist session expiry and renewal, define late-success reconciliation without restoring cancelled bookings, test duplicate/late callbacks. | Yes: refund and cancellation policy. |
| G-08 | Money exactness | `Booking.feeAmountVnd` is integer; `PaymentTransaction.amount` is `Float` in Prisma. | Exact VND comparisons may eventually drift or require unsafe rounding. | P1 | Plan additive exact-money migration and data verification before changing authoritative amount storage. | No if VND-only remains canonical. |
| G-09 | Public booking UX and timezone | `GuestQuickBookingPanel.tsx` is one long form and computes today using UTC `toISOString().slice(0, 10)` despite existing Vietnam timezone utility. | Wrong default date near midnight in Vietnam and heavy first-use form. | P1/P2 | Use canonical Vietnam date utility; present resource/time, customer details, and OTP/review as clear steps after backend gates are corrected. | Address requirement for room-only booking is open. |
| G-10 | Deployment configuration | Current state report records absent local SMTP/VNPAY credentials. External audit finds env/deploy docs lagging new OTP/payment/media behavior. | Live OTP and real VNPAY Sandbox cannot be proven from current local environment. | P1 | Complete env examples/runbook and execute real isolated SMTP and merchant/IPN verification when credentials and reachable callback exist. | Whether VNPAY is in final live demo. |

## Boundary and next action

This report completes Phase 0's evidence matrix. No runtime code, schema, migration, or GitHub PR was changed. The first implementation phase is G-01 only. The byte-level cause is established by the tracked blobs, `.gitattributes`, and hash comparison; an isolated PostgreSQL 16 run is still required to validate the deployment mechanism and the values Prisma records. Do not edit historical migration SQL or `_prisma_migrations` to force a green result.
