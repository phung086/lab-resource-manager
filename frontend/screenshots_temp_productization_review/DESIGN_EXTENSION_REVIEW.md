# Design extension evidence

Inline documentation fallback: the finish handoff could not continue because sub-agent usage was exhausted. This ordinary extension preserves the incumbent system and honors the user's boundary against canonical documentation changes.

No changes to canonical design/closure documentation. Checked `PRODUCT.md`, `DIRECTION.md`, `frontend/src/styles/light-redesign.css`, `public-landing.css`, `PublicLanding.tsx`, `AuthRegisterView.tsx`, `WorkspaceHome.tsx`, resource-category symbols, assistant and payment components/CSS, and rendered review captures.

Palette: inherited white and cool gray workspace surfaces, navy text and blue actions; public tokens are `--public-ink: #183450` and `--public-blue: #215db1`, with pale blue sections and `#173c64` role field.

Type ramp: existing locally bundled Plus Jakarta Sans; public h1 clamp 36–53px (35px on small phones), h2 28–39px, h3 16–17px, body 14–16px; IBM Plex Mono remains reserved for inherited technical identifiers.

Layout: 1200px public container, alternating two-column editorial sections, five-category strip, six ordered workflow steps, four-role section; 1100/850/500px responsive rules; mobile disclosure navigation; existing operational sidebar/topbar preserved.

Components: public CTA minimum height 48px with 8px radius; 16px illustration/section radius; 3px visible focus outline; readable assistant cards and payment timeline use real data; existing modal focus handling remains in place.

Named rules: illustrations disclose that they are illustrative; public content never invents partners/statistics/live telemetry; local structured answers hide redundant raw lists under details; optional payment never implies automatic fee or verified success without evidence; reduced-motion users receive static category icons.

Not canonized or repaired: legacy utility classes, uppercase labels elsewhere in inherited operational modules, pre-existing lint warnings, and mixed CSS token conventions. They are not new design rules and remain outside this bounded extension. No new raster asset ships; custom SVG geometry is original source and Lucide icons come from the existing installed package.
